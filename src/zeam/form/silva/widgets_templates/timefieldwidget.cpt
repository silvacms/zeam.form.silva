<span tal:define="id widget.htmlId()"
      i18n:domain="silva">
  <input type="text" size="8" value="${widget.inputValue()}" id="${id}" class="${widget.htmlClass()}"
         name="${widget.identifier}" />
</span>
